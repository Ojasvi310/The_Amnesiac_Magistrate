# Local/CPU dependencies only.
# DO NOT install these in the Colab session — llama-cpp-python's CPU wheel conflicts
# with the GPU torch build that bitsandbytes expects, and streamlit/fastapi have no
# business running in an ephemeral training session.
#
# Install locally with:
#   pip install -r requirements-online.txt

# GGUF inference (CPU path — this is the whole point of the quantization step)
# Install the CPU-only wheel; if you accidentally install the GPU wheel the
# no-network Docker smoke test will still pass but latency numbers won't be honest.
llama-cpp-python>=0.2.57

# Retrieval
faiss-cpu>=1.8.0
sentence-transformers>=2.7.0

# Serving
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Dashboard
streamlit>=1.35.0

# Config + persistence
pyyaml>=6.0.1
sqlalchemy>=2.0.29            # ORM layer over SQLite for registry + audit log

# Utilities
numpy>=1.26.0
pandas>=2.2.0
httpx>=0.27.0                 # for local test client in api.py tests ONLY; never imported in src/online/
